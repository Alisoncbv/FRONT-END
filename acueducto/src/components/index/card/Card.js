import React from "react";
import './Card.css';

const Card = ( ) => {
    return(
        <div className="container-card">
            <div>
                <img
                    className="imgCard"
                    src=""
                />
            </div>

        </div>
    );
}

export default Card;